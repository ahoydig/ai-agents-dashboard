# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Agent API URL - Base URL for agent endpoints
# The playground will call {AGENTS_API_URL}/playground/execute
# Expected response format includes:
#   - response: string
#   - config_used: { model, temperature, system_prompt }
#   - execution_logs: [{ step, message, duration_ms, status, details }]
#   - metrics: { tokens_in, tokens_out, cost_usd, latency_total_ms }
#   - tool_calls, guardrails
AGENTS_API_URL=https://centerfisio.ai-agents.ahoy.digital/api

# GitHub Integration (optional - for creating issues from playground)
GITHUB_TOKEN=ghp_your_token_here
GITHUB_OWNER=your-org
GITHUB_REPO=your-repo
